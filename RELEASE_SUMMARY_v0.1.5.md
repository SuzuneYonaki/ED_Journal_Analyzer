# Elite Dangerous Journal Analyzer v0.1.5 Release Notes

## 🌌 Web共有HTMLの「完全天体物理観測JSON内包」＆ AI推論対応・保存先自動特定リリース

本バージョン（**v0.1.5**）では、Web共有HTML（`{星系名}_share.html`）を「人間向けの美しいスタンドアロン星系図ビューアー」から、**「各種生成AI（ChatGPT、Claude、Gemini等）にそのままアップロードして星系形成史シナリオや天体物理学的考察を100%の精度で推論させることができる完全データコンテナ」**へと進化させました。
また、デスクトップ環境で出力先が分からなくなる問題を解消するため、専用 `exports/` フォルダーへの自動保存および Windows エクスプローラーでの自動ハイライト表示機能を実装しました。

---

### 🚀 主な新機能・改善点

#### 1. Web共有HTMLへの「完全天体物理観測JSONデータ」内包 (`<script id="ed-system-astrophysics-data">`)
- **完全データコンテナ化**:
  - 生成される HTML 内の `<head>` に、星系および全天体の完全な天体物理・軌道観測データ（恒星・惑星質量、精密半径、軌道長半径、離心率、公転・自転周期、軸傾斜、詳細大気組成比率、地質・生体シグナル、Rhino採掘地点）を JSON 形式でまるごと埋め込みました。
- **生成AI（LLM）への直接投入と星系形成史推論**:
  - **使い方**: 生成された `{星系名}_share.html` ファイルをそのまま ChatGPT、Claude、Gemini 等にドラッグ＆ドロップ（添付）し、README に掲載されているプロンプトを入力するだけで、現代天文学・惑星科学に基づいた学術的考察や、時系列に沿った星系形成史シナリオ、天球・景観の描写を推論させることができます。
- **天体テーブルのアコーディオン展開 (`<details>`)**:
  - ブラウザで開いた際、各天体の行に「🔬 詳細天体物理パラメータ (AI推論用)」アコーディオンを新設。質量、半径、AU軌道長半径、離心率、公転周期、大気組成比率を人間もワンクリックで即座に直接確認できます。
- **AI推論対応バナー**:
  - HTML の最上部に、本ファイルが完全観測 JSON を内包した AI 推論対応データである旨を明記した案内カードを設置しました。

#### 2. 専用 `exports/` フォルダーへの自動保存 ＆ エクスプローラー直接オープン
- **保存先の可視化**:
  - 出力された HTML は、アプリケーション実行フォルダー直下の **`exports/`** ディレクトリへ確実に自動書き出しされます。
- **エクスプローラー自動起動**:
  - 出力完了と同時に、Windows エクスプローラーが自動で起動し、保存された HTML ファイルを選択（ハイライト）した状態でフォルダーが開きます。保存場所を探す手間がゼロになります。
- **画面上トースト通知**:
  - アプリ画面右下にも保存先フルパスと「📂 保存フォルダーを開く」ボタンを表示し、いつでも保存場所にアクセス可能です。

---

### 🛠️ 検証と品質保証

- **自動テスト全92件 PASS**:
  - スタンドアロン HTML 内への完全 JSON 埋め込み、AIバナーの存在、クリーンなタグ構造、エクスポート API の動作確認テストを含め、全件正常パス（`92 passed`）。
- **配布用パッケージビルド完了**:
  - `dist/ED_Journal_Analyzer.exe`（PyInstaller 再ビルド完了）
  - `dist/ED_Journal_Analyzer_v0.1.5.zip`
  - `dist/ED_Journal_Analyzer.zip`（最新版エイリアス）
- **Git リモート同期完了**:
  - コミット `b967939` を `origin/main` にプッシュ完了。
